import { defineStyleConfig } from '@chakra-ui/react'

const style = defineStyleConfig({
  baseStyle: {},
  sizes: {},
  variants: {},
  defaultProps: {
    size: 'sm',
    variant: '',
    colorScheme: '',
  },
})

export default style
