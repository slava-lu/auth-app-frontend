import { defineStyleConfig } from '@chakra-ui/react'

const style = defineStyleConfig({
  baseStyle: {
    flexShrink: 0,
  },
  sizes: {},
  variants: {},
  defaultProps: {
    size: 'sm',
    variant: 'solid',
    colorScheme: '',
  },
})

export default style
